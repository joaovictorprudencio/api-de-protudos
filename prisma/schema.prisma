
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Produto {
  @@map("produtos")
  id String @id
  nome String
  preco Float
  quantidade Int
}
